attributes:
  select:
    obi_network_flow_bytes:
      include:
      - obi.ip
      - src.address
      - dst.address
      - src.name
      - dst.name
      - src.namespace
      - dst.namespace
      - src.cidr
      - dst.cidr
      - iface
      - iface_direction
      - direction
